export enum Role {
    /**
     * 客户端
     */
    CLIENT,
    /**
     * 服务器
     */
    SERVER,
    /**
     * 其它
     */
    OTHER,
}

export enum SvnEvent {
    COMMIT,
    UPDATE,
    LOCK,
    UNLOCK,
}
